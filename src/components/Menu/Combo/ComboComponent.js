import React from 'react';
import PropTypes from 'prop-types';
import './combo.scss';

export default class ComboComponent extends React.PureComponent {
  constructor(props) {
    super(props);
    this.props = props;
  }

  render() {
    return (
      <div>
      </div>
    );
  }
}

ComboComponent.propTypes = {
};